[project]
name = "arcgeneral-sandbox"
version = "0.1.0"
description = "Sandbox kernel for arcgeneral"
requires-python = ">=3.11,<3.15"
dependencies = [{dependencies}
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
